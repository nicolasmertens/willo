# Archived klas games

## Big vs Small (`sort/`) — archived 2026-08-11

William (~21 months) did not grasp the big/small concept on this surface.
Parked rather than deleted in case we redesign the angle later
(e.g. size with real toys, fewer animals, parent-led demo).

To restore: move `sort/` back to `klas/games/sort/`, restore `hero-sort.jpg`,
re-add the tile in `tracks/klas-games.json`, run `python3 render.py klas`.
